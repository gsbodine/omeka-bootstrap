<div id="admin-bar-container" class="navbar navbar-static-top">
    <nav id="admin-bar pull-right">
    
    <?php //if($user = current_user()) {
        /*
        $links = array(
            array(
                'label' => __('Welcome, %s', $user->name),
                'uri' => admin_url('/users/edit/'.$user->id)
            ),
            array(
                'label' => __('Omeka Admin'),
                'uri' => admin_url('/')
            ),
            array(
                'label' => __('Log Out'),
                'uri' => url('/users/logout')
            )
        );

    } else {
        $links = array();
    }

    $nav = nav($links, 'public_navigation_admin_bar');
    echo $nav->setUlClass('nav navbar-inner');
         * 
         */
    ?> 
    </nav>
</div>

